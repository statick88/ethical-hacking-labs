version: '3.8'

services:
  kali:
    build: .
    container_name: kali-unit2
    ports:
      - "2223:22"
      - "8081:8080"
    volumes:
      - ./data:/root/data
    environment:
      - TZ=Europe/Madrid
    privileged: true
    restart: unless-stopped
