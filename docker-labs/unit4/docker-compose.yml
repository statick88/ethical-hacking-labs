version: '3.8'

services:
  kali:
    build: .
    container_name: kali-unit4
    ports:
      - "2225:22"
      - "8083:8080"
    volumes:
      - ./data:/root/data
    environment:
      - TZ=Europe/Madrid
    privileged: true
    restart: unless-stopped
