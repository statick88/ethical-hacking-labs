version: '3.8'

services:
  kali:
    build: .
    container_name: kali-unit3
    ports:
      - "2224:22"
      - "8082:8080"
    volumes:
      - ./data:/root/data
    environment:
      - TZ=Europe/Madrid
    privileged: true
    restart: unless-stopped
