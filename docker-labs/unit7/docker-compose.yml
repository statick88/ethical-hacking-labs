version: '3.8'

services:
  kali:
    build: .
    container_name: kali-unit7
    ports:
      - "2228:22"
      - "8086:8080"
    volumes:
      - ./data:/root/data
    environment:
      - TZ=Europe/Madrid
    privileged: true
    restart: unless-stopped
