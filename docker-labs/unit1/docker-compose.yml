version: '3.8'

services:
  kali:
    build: .
    container_name: kali-unit1
    ports:
      - "2222:22"
      - "8080:8080"
    volumes:
      - ./data:/root/data
    environment:
      - TZ=Europe/Madrid
    privileged: true
    restart: unless-stopped
